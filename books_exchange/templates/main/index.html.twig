{% extends 'base.html.twig' %}

{% block title %}Page d'accueil du site d'ÉchangeLivres
{% endblock %}

{% block body %}
	{% for message in app.flashes('success') %}
		<div class="alert alert-success" role="alert">{{ message }}</div>
	{% endfor %}
	<section class="contentBlockSection">
		<section class="introductoryContent">
			<h2 class="h2IntroductoryContent">Échangez vos livres contre des nouveaux avec ÉchangeLivres !</h2>
			<p>Facile et économique, le troc de livres d'occasion vous permet de lire de nouveaux titres toute l'année. Inscrivez-vous gratuitement et ajoutez vos livres à notre catalogue composé de milliers d'ouvrages.</p>
		</section>

		<section class="instructionsForUse">
			<h3 class="h3InstructionsForUse">L'échange de livres, mode d'emploi</h3>
			<p>Vous êtes nouveau et souhaitez échanger vos livres sur Internet ? Découvrez comment fonctionne l'échange de livres sur ÉchangeLivres.</p>
			<img src="./img/imageForTheInstructionManualBlock.svg" class="imageForTheInstructionManualBlock" alt="image For The Instruction Manual Block"/>
			<a href="{{ path('app_how_it_works') }}" class="linkOnHowTheWebsiteWorks">Cliquez ici pour voir le fonctionnement de l'échange de livres</a>
		</section>
		<section class="sectionOfBookSelection">
			<h2 class="h2TitleSelectionOfBooks">Une sélection de livres disponibles à l'échange</h2>
			<div class="divDisplayBlocksBookInformation">
				{% for book in books %}
					<section class="row col-sm-12 blockOfTheBookInformation">
						<h4 class="col-sm-12">{{ book.title }}</h4>
						<div class="col-sm-12 line author">
							<a href="">{{ book.author }}</a>
						</div>
						<p class="col-sm-12 line category">{{ book.category }}</p>
						<p class="col-sm-12 line publisher">{{ book.publisher }}</p>
						<p class="col-sm-12 line language">Langue:
							{{ book.language }}</p>
						<p class="col-sm-12 line format">Format:
							{{ book.format }}</p>
						<p class="col-sm-12 line state">État:
							{{ book.state }}</p>
						<div class="line location">
							<img src="{{ asset('/img/locationIcon.svg') }}" class="locationIcon" alt="location Icon"/>
							<span>{{ book.user.city }}</span>
						</div>
						<p class="col-sm-12 line owner">Propriétaire:
							{{ book.user.pseudo }}
						</p>
						<div class="divExchangeButton">
							<a href="{{ path('user_book_validation_confirm_exchange', {'id': book.id}) }}" class="exchangeButton" role="button">Échanger</a>
						</div>
					</section>
				{% endfor %}
			</div>
		</section>
	</section>
{% endblock %}
