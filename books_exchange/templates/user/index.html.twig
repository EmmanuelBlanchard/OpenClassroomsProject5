{% extends 'user.html.twig' %}

{% block title %}Hello UserController!
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row col-sm-6">
			<div class="col-sm-12">
				<h1>Profil de
					{{ app.user.firstname }}
					{{ app.user.lastname }}
				</h1>
			</div>
			<div class="col-sm-2">Nom :</div>
			<div class="col-sm-10">{{ app.user.lastname }}</div>
			<div class="col-sm-2">Prénom :</div>
			<div class="col-sm-10">{{ app.user.firstname }}</div>
			<div class="col-sm-2">E-mail :</div>
			<div class="col-sm-10">{{ app.user.email }}</div>
		</div>
	</div>
{% endblock %}
