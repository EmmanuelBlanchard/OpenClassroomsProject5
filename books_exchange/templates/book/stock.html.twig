{% extends 'base.html.twig' %}

{% block title %}
	Stock de livres de
	{{ app.user.pseudo }}
{% endblock %}

{% block body %}
	<div class="container">
		{% for message in app.flashes('message') %}
			<div class="divAlert col-sm-12">
				<div class="alert alert-success" role="alert">
					{{message}}
				</div>
			</div>
		{% endfor %}
		{% for message in app.flashes('error') %}
			<div class="divAlert col-sm-12">
				<div class="alert alert-danger" role="alert">
					{{ message }}
				</div>
			</div>
		{% endfor %}
		<h2 class="h2ConnectedBookStockAddABook">Stock de livres</h2>

		<div class="col-sm-12">
			<h3 class="h3ConnectedBookStockAddABook">Bibliothèque de
				{{ app.user.firstname }}
				{{ app.user.lastname }}
				{% if books|length == "0" %}
					(0 livre)
				{% else %}
					({{ books|length }}
					livres)
				{% endif %}
			</h3>
		</div>

		{% if books|length == "0" %}
			<div class="divConnectedBookStockAddABook">
				<p>Votre bibliothèque est vide.</p>
				<p>Complétez-là avec les livres que vous possèdez</p>
				<p>et que vous souhaitez échanger.</p>
			</div>

			<div class="divButtonConnectedBookStockAddABook">
				<a class="btn btn-success" href="{{ path('book_add') }}" role="button">Ajouter un livre</a>
			</div>
		{% else %}
			<div class="divButtonConnectedBookStockAddABook">
				<a class="btn btn-success" href="{{ path('book_add') }}" role="button">Ajouter un livre</a>
			</div>

			<div class="row col-sm-12">
				{% for book in books %}
					<div class="bookCard col-sm-4">

						<div class="titleBook">{{ book.title|u.truncate(24, '...') }}</div>
						<div class="authorBook">
							<a href="">{{ book.author }}</a>
						</div>
						<div class="categoryBook">{{ book.category }}</div>
						<div class="publisherBook">{{ book.publisher }}</div>
						<div class="languageBook">{{ book.language }}</div>
						<div class="formatBook">{{ book.format }}</div>
						<div class="stateBook">{{ book.state }}</div>

						<div class="align-self-center">
							<a class="btn btn-outline-secondary" href="{{ path('book_remove_stock', {'id': book.id }) }}" role="button">Supprimer</a>
						</div>

					</div>
				{% endfor %}
			</div>
		{% endif %}

		<nav class="page-navigation" aria-label="Page navigation">
			<ul class="pagination">
				<li class="page-item {{ (page == 1) ? 'disabled' : '' }}">
					<a class="page-link" href="?page=1" aria-label="Première Page">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-chevron-double-left" viewBox="0 0 16 16">
							<path fill-rule="evenodd" d="M8.354 1.646a.5.5 0 0 1 0 .708L2.707 8l5.647 5.646a.5.5 0 0 1-.708.708l-6-6a.5.5 0 0 1 0-.708l6-6a.5.5 0 0 1 .708 0z"/>
							<path fill-rule="evenodd" d="M12.354 1.646a.5.5 0 0 1 0 .708L6.707 8l5.647 5.646a.5.5 0 0 1-.708.708l-6-6a.5.5 0 0 1 0-.708l6-6a.5.5 0 0 1 .708 0z"/>
						</svg>
					</a>
				</li>
				<li class="page-item {{ (page == 1) ? 'disabled' : '' }}">
					<a class="page-link" href="{{ (page > 1) ? '?page=' ~ (page - 1) : '' }}" aria-label="Page Précédente">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-chevron-left" viewBox="0 0 16 16">
  							<path fill-rule="evenodd" d="M11.354 1.646a.5.5 0 0 1 0 .708L5.707 8l5.647 5.646a.5.5 0 0 1-.708.708l-6-6a.5.5 0 0 1 0-.708l6-6a.5.5 0 0 1 .708 0z"/>
						</svg>
					</a>
				</li>
				{% set pages = (total / limit)|round(0, 'ceil') %}
				{% for item in 1..pages %}
					<li class="page-item {{ (page == item) ? 'active' : '' }}">
						<a class="page-link" href="?page={{ item }}">{{ item }}</a>
					</li>
				{% endfor %}
				<li class="page-item {{ (page == pages) ? 'disabled' : '' }}">
					<a class="page-link" href="{{ (page < pages) ? '?page=' ~ (page +1) : '' }}" aria-label="Page Suivante">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-chevron-right" viewBox="0 0 16 16">
  							<path fill-rule="evenodd" d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"/>
						</svg>
					</a>
				</li>
				<li class="page-item {{ (page == pages) ? 'disabled' : '' }}">
					<a class="page-link" href="{{ pages }}" aria-label="Dernière Page">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-chevron-double-right" viewBox="0 0 16 16">
  							<path fill-rule="evenodd" d="M3.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L9.293 8 3.646 2.354a.5.5 0 0 1 0-.708z"/>
  							<path fill-rule="evenodd" d="M7.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L13.293 8 7.646 2.354a.5.5 0 0 1 0-.708z"/>
						</svg>
					</a>
				</li>
			</ul>
		</nav>
		
	</div>
{% endblock %}
