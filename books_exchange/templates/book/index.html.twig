{% extends 'base.html.twig' %}

{% block title %}
	Liste de livres
{% endblock %}

{% block body %}
	<section class="container bookIndex">
		{% for message in app.flashes('message') %}
			<div class="divAlert">
				<div class="alert alert-success" role="alert">
					{{message}}
				</div>
			</div>
		{% endfor %}
		{% for message in app.flashes('error') %}
			<div class="divAlert">
				<div class="alert alert-danger" role="alert">
					{{ message }}
				</div>
			</div>
		{% endfor %}
		<h2 class="h2BookIndex">Liste de livres</h2>

		<div class="row blockCard" id="content">
			{% include "book/_content.html.twig" %}
		</div>
	</section>
{% endblock %}