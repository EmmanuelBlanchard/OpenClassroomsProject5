{% extends 'admin/index.html.twig' %}

{% block title %}Gérer les langues
{% endblock %}

{% block body %}
	<div class="container">
		{% for message in app.flashes('message') %}
			<div class="divAlert col-sm-12">
				<div class="alert alert-success" role="alert">
					{{message}}
				</div>
			</div>
		{% endfor %}
		{% for message in app.flashes('error') %}
			<div class="divAlert col-sm-12">
				<div class="alert alert-danger" role="alert">
					{{ message }}
				</div>
			</div>
		{% endfor %}
		<h1>Gérer les langues</h1>
		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th scope="col">ID</th>
					<th scope="col">Nom</th>
					<th scope="col">Parent</th>
					<th scope="col">Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for item in language %}
					<tr>
						<th scope="row">{{item.id}}</th>
						<td>{{item.name}}</td>
						<td>{{item.parent}}</td>
						<td class="text-center">
							<a class="btn btn-primary m-2" href="{{ path("admin_language_add") }}" role="button">Ajouter</a>
							<a class="btn btn-primary m-2" href="{{ path("admin_language_update", {id: item.id}) }}" role="button">Modifier</a>
							<a class="btn btn-primary m-2" href="{{ path("admin_language_delete", {id: item.id}) }}" role="button">Supprimer</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>

{% endblock %}
