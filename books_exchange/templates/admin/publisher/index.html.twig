{% extends 'baseBootstrap.html.twig' %}

{% block title %}Gérer les éditeurs
{% endblock %}

{% block body %}
	<div class="container">

		<h1>Gérer les éditeurs</h1>

		<a class="btn btn-primary" href="{{ path("admin_publisher_add") }}" role="button">Ajouter un éditeur</a>

		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th scope="col">ID</th>
					<th scope="col">Nom</th>
					<th scope="col">Parent</th>
					<th scope="col">Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for item in publisher %}
					<tr>
						<th scope="row">{{item.id}}</th>
						<td>{{item.name}}</td>
						<td>{{item.parent}}</td>
						<td class="text-center">
							<a class="btn btn-primary" href="{{ path("admin_publisher_update", {id: item.id}) }}" role="button">Modifier</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>

{% endblock %}
