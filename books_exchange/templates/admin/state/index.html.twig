{% extends 'baseBootstrap.html.twig' %}

{% block title %}Gérer les états du livre
{% endblock %}

{% block body %}
	<div class="container">

		<h1>Gérer les états du livre</h1>

		<a class="btn btn-primary" href="{{ path("admin_state_add") }}" role="button">Ajouter un état du livre</a>

		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th scope="col">ID</th>
					<th scope="col">Nom</th>
					<th scope="col">Parent</th>
					<th scope="col">Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for item in state %}
					<tr>
						<th scope="row">{{item.id}}</th>
						<td>{{item.name}}</td>
						<td>{{item.parent}}</td>
						<td class="text-center">
							<a class="btn btn-primary" href="{{ path("admin_state_update", {id: item.id}) }}" role="button">Modifier</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>

{% endblock %}
